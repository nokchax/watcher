spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:file:~/watcher
    driver-class-name: org.h2.Driver

telegram:
  bot:
    name:
    token:
